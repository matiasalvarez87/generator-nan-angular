language: node_js
node_js:
  - '0.8'
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-nan-angular' ]; then cd .. && eval "mv $currentfolder generator-nan-angular" && cd generator-nan-angular; fi

